apply plugin: 'java-library'

ext.moduleName = 'com.ssi.clean.example.config'

dependencies {
	implementation(
		project(':usecase'),
		project(':entities'),
		project(':in-memory-db-simple'),
		project(':in-memory-db-hazelcast'),
		project(':jug'),
		project(':uuid'),
		project(':encoder-sha256'),
		project(':controller'),
	)
}
